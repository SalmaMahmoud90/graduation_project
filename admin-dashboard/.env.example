# Optional. Leave unset (or empty) while running `npm run dev` so requests use
# the Vite proxy and stay on the same origin as the dashboard.
#
# Set this only when the dashboard is hosted separately from the server, e.g.:
VITE_API_BASE_URL=http://76.13.9.191:3012
