enum PageAnimation { slide, fade, none }

enum UserType { driver, rider }

enum ReportCategory { spam, harassement, fake, dangerous, other}

enum ReportStatus { pending, reviewed }

enum TransactionType {deposite, payment, earning, refund}

enum PaymentMethod {syriatel_cach, sham_cach}

enum DepositeRequestStatus {pending, approved, rejected}

enum ReservationPaymentStatus {unpaid, paid}